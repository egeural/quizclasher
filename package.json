{
    "name": "quiz-clasher-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "node server/server.js",
        "build": "npm run build --prefix client",
        "postinstall": "npm install --prefix server && npm install --prefix client"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}